#Installer
dependencies:
  pre:
    - sudo apt-get update; sudo apt-get install openssl libapr1 curl
test:
  override:
    - ./helpers/sgprocess.sh 5.0.2
    - ./helpers/sgprocess.sh 5.1.2
    - ./helpers/sgprocess.sh 5.2.2
    - ./helpers/sgprocess.sh 5.3.2
    - ./helpers/sgprocess.sh 5.4.0